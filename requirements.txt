# FULL REQUIREMENTS (LOCAL DEVELOPMENT)
# Includes all dependencies including heavy ones for full functionality

# Core Streamlit and web framework
streamlit>=1.28.0

# Data manipulation and analysis
pandas>=1.5.0
numpy>=1.24.0

# HTTP requests
requests>=2.28.0

# PDF processing and OCR (heavy dependencies - may not work in all cloud environments)
camelot-py[cv]>=0.10.1
easyocr>=1.7.0
PyPDF2>=3.0.0
pdf2image>=1.16.0
pdfplumber>=0.7.0

# Image processing
Pillow>=9.5.0

# Enhanced UI components (optional but used in project)
streamlit-antd-components>=0.2.0
streamlit-tags>=1.2.8
streamlit-tile>=0.1.0

# Note: For cloud deployment, use requirements-cloud.txt instead
# Heavy dependencies like camelot-py, easyocr, and pdf2image
# require system libraries that may not be available in cloud environments
