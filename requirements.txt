# Core Streamlit and web framework
streamlit>=1.28.0

# Data manipulation and analysis
pandas>=1.5.0
numpy>=1.24.0

# HTTP requests
requests>=2.28.0

# PDF processing and OCR
camelot-py[cv]>=0.10.1
easyocr>=1.7.0
PyPDF2>=3.0.0

# Image processing
Pillow>=9.5.0

# Optional: Machine learning backend for EasyOCR
torch>=2.0.0
torchvision>=0.15.0

# Enhanced UI components (optional but used in project)
streamlit-antd-components>=0.2.0
streamlit-tags>=1.2.8
streamlit-tile>=0.1.0

# Standard library dependencies (included for completeness, usually pre-installed)
# These are typically included with Python but listed for clarity:
# os - built-in
# re - built-in  
# json - built-in
# zipfile - built-in
# tempfile - built-in
# mimetypes - built-in
# datetime - built-in
# typing - built-in (Python 3.5+)
# io - built-in
# importlib - built-in
# sys - built-in
# time - built-in
